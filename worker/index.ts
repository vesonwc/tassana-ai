// Worker skeleton — real pipeline lands in M2 (LINE) and M3 (VLM).
// Will consume the pgmq "events" queue and deliver LINE alerts.

console.log("tassana-ai worker: not implemented yet (starts in M2)");
