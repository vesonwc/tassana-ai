# dependencies
node_modules/

# next.js
.next/
out/

# production
build/

# env — never commit secrets
.env
.env.*
!.env.example

# misc
*.log
.DS_Store
*.tsbuildinfo
next-env.d.ts

# supabase local
supabase/.temp/

# starter docs (duplicated into docs/) + Office temp files
startup/
~$*

# personal test images
ทดลองใช้/

# accuracy test images and results (local only)
testdata/

# site photos / personal reference images (PDPA - never in repo)
ข้อมูลกล้อง/
nvr-snapshots/

logs/

# generated by install-autostart.ps1 on the bridge PC
worker/agent/run-nvr-listener.cmd
worker/agent/run-nvr-listener-hidden.vbs
logs/

# ONNX detector weights — downloaded at runtime (ADR-015)
models/
